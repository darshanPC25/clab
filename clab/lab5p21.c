//WAP in c to reverse the given number.
#include<stdio.h>

int main(){
    int num,dig,a;
    printf("Enter The number: ");
    scanf("%d",&num);

    while(num>0){
        a=num%10;
        num/=10;
        dig=a*10
    }
}